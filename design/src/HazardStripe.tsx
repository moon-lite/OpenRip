/**
 * Diagonal hazard-stripe divider in the accent color. A full-width section
 * separator; use sparingly — one per screen, typically under the header.
 */
export function HazardStripe() {
  return <div className="or-hazard" aria-hidden="true" />;
}
